<Window x:Class="DynamicReportSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Dynamic Reporting System"
        Height="620"
        Width="1100"
        MinHeight="520"
        MinWidth="860"
        Loaded="Window_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Text="Hệ thống tạo báo cáo động" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Padding="10"
                    BorderBrush="#D9D9D9"
                    BorderThickness="1"
                    CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="txtSelectedTable"
                               Grid.Row="0"
                               Margin="0,0,0,8"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Text="Preview dữ liệu" />

                    <DataGrid x:Name="dgData"
                              Grid.Row="1"
                              AutoGenerateColumns="True"
                              IsReadOnly="True"
                              CanUserAddRows="False" />
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="#ECECEC" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Padding="10"
                        BorderBrush="#D9D9D9"
                        BorderThickness="1"
                        CornerRadius="6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Margin="0,0,0,8"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Text="Danh sách bảng" />

                        <ListBox x:Name="lstTables"
                                 Grid.Row="1"
                                 MouseDoubleClick="lstTables_MouseDoubleClick" />
                    </Grid>
                </Border>

                <Border Grid.Row="2"
                        Padding="10"
                        BorderBrush="#D9D9D9"
                        BorderThickness="1"
                        CornerRadius="6">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Margin="0,0,0,8"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Text="Danh sách field/cột" />

                        <ListBox x:Name="lstColumns"
                                 Grid.Row="1"
                                 DisplayMemberPath="DisplayText" />
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
